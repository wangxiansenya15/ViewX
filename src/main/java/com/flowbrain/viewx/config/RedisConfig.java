package com.flowbrain.viewx.config;

import com.flowbrain.viewx.util.CustomJsonRedisSerializer;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.data.redis.connection.RedisConnectionFactory;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.data.redis.serializer.StringRedisSerializer;

//  Redis配置类   Generated By DeepSeek V3
@Configuration
public class RedisConfig {

    @Bean
    public RedisTemplate<String, Object> redisTemplate(RedisConnectionFactory factory) {
        RedisTemplate<String, Object> template = new RedisTemplate<>();
        template.setConnectionFactory(factory);

        // Key采用字符串序列化
        template.setKeySerializer(new StringRedisSerializer());
        template.setHashKeySerializer(new StringRedisSerializer());

        // Value采用自定义JSON序列化 - 禁用默认类型信息
        template.setValueSerializer(new CustomJsonRedisSerializer<>(Object.class, false));
        template.setHashValueSerializer(new CustomJsonRedisSerializer<>(Object.class, false));

        template.afterPropertiesSet();
        return template;
    }
}